apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: platform-configs
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-5"
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - git:
        repoURL: https://github.com/catdevdev/gitops-template-argocd.git
        revision: main
        files:
          - path: platform-apps/configs/*.yaml
  template:
    metadata:
      name: "{{.name}}"
      namespace: argocd
      labels:
        app.kubernetes.io/part-of: single-cluster
        gitops.layer: platform-configs
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: single-cluster
      source:
        repoURL: https://github.com/catdevdev/gitops-template-argocd.git
        targetRevision: main
        path: "{{.sourcePath}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{.namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - SkipDryRunOnMissingResource=true
