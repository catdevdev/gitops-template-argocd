apiVersion: v1
kind: Service
metadata:
  name: sample-web
  namespace: sample-web
  labels:
    app.kubernetes.io/name: sample-web
    app.kubernetes.io/part-of: single-cluster
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sample-web
  ports:
    - name: http
      port: 80
      targetPort: http
